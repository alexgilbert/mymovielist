<% provide(:title, 'Create List') %>
<h1>New List</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@list) do |f| %>
      <%= render  'fields', f: f %>
      <%= f.submit "Create", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
